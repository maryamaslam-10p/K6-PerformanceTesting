pipeline {
    agent any

    environment {
        // K6 web dashboard settings
        K6_WEB_DASHBOARD = "true"
        K6_WEB_DASHBOARD_EXPORT = "report.html"
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/maryamaslam-10p/K6-PerformanceTesting'
            }
        }

        stage('Set Credentials & Run k6 Test') {
            steps {
                // Use Jenkins credentials as environment variables
                withCredentials([
                    string(credentialsId: 'BASE_URL', variable: 'BASE_URL'),
                    usernamePassword(credentialsId: 'API_USER', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')
                ]) {
                    bat """
                        cd tests
                        echo BASE_URL=%BASE_URL%
                        echo USERNAME=%USERNAME%
                        echo PASSWORD=%PASSWORD%
                        k6 run main.js
                    """
                }
            }
        }

        stage('Publish Report') {
            steps {
                publishHTML(target: [
                    allowMissing: false,
                    keepAll: true,
                    reportDir: 'tests',
                    reportFiles: 'report.html',
                    reportName: 'k6 Performance Report'
                ])
            }
        }
    }
}
